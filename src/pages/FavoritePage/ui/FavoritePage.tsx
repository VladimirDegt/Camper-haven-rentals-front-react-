import { FavoriteList } from '@/features/FavoriteList/FavoriteList';

const FavoritePage = () => {

    return (
        <main>
            <FavoriteList/>
        </main>
    )
};

export default FavoritePage;
